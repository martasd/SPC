/*****************************************************************************
 * file:        TODO
 * authors:     Martin Dluhos
 * created:     December 11, 2011
 * revised:     December 14, 2011
 * description: Keeps track of progress and maintains a list of features to
 *              implement in the future.
 ****************************************************************************/

===Phase 7: Function/Procedure Calls===
* not started

===Phase 6: Control Structures===
* make sure that instruction generated for children of simple_statement gets build at the children nodes and gets retrieved
* and generated at grand (n)-parent nodes

===Phase 5: Simple Code Generation===
* fix WRITELN

===Phase 4: Type Checker===
* type checking in functions

==Phase 3: Symbol Table===
* completed

===Phase 2: Parsing===
* complete (using Sam's version)

translate(IF bexp THEN statement1 ELSE statement2)
  consequent = gen_label();
  alternate = gen_label();
  end = gen_label ();
  translate (bexp, consequent, alternate)
  generate_instruction (_LABEL, consequent, NULL, NULL);
  translate (statement1)
  generate_instruction (JUMP, end, NULL, NULL);
  generate_instruction (_LABEL, alternate, NULL, NULL);
  translate (statement2)
  generate_instruction (JUMP, end, NULL, NULL);
  generate_instruction (_LABEL, end, NULL, NULL);

translate (id, truelabel, falselabel)
  JEQI falselabel id 0
  JUMP truelabel
